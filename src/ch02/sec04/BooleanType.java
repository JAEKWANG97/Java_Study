package ch02.sec04;

public class BooleanType {
    public static void main(String[] args) {
        // 참 거짓을 의미하는 논리 리터럴은 True와 false이다. 논리 리터럴은 Boolean 타입 변수에 다음과 같이 대입한다.
        boolean stop = true;
//        boolean stop = false;
        // boolean 타입 변수는 주로 두 가지 상태값을 저장할 필요가 있을 경우에 사용되며, 상태값에 따라 조건문과 제어문의 실행 흐름을 변경하는데 사용된다.

        int x = 10;
        /*
        * boolean result = (x == 20);
        * boolean result = (x != 20);
        * boolean result = (x > 20);
        * boolean result = (0 < x && x < 20);
        * boolean result = (x < 0 || x > 200);
        *  */
    }
}
