package ch02.sec06;

public class StringType {
    public static void main(String[] args) {
        // 작은 따옴표로 감싼 한개의 문자는 char타입
        // 큰 따옴표로 깜산 여러개의 문자들은 유니코드로 변환이 되지 않음
        String var1 = "A";
        String var2 = "홍길동";

        // String 타입은 자바 기본 타입에 속하지 않는 참조 타입이다.

        // 문자열 내부에서 역슬래쉬가 붙은 문자가 있음 이는 이스케이프 문자라고함



    }
}
